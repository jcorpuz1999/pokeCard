import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    card: {
        width: '48.5%',
        justifyContent: 'center',
        alignItems: 'center',
        borderColor: '#FFFFFF',
        borderStyle: 'solid',
        borderWidth: 0.5,
        marginLeft: 6,
        marginBottom: 12
    }
})